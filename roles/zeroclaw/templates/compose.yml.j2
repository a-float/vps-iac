services:
  zeroclaw:
    image: ghcr.io/zeroclaw-labs/zeroclaw:latest
    container_name: zeroclaw
    restart: unless-stopped

    ports:
      - "127.0.0.1:42617:42617"

    volumes:
      - ./data:/zeroclaw-data

    environment:
      OPENROUTER_API_KEY: "{{ zeroclaw_openrouter_api_key }}"